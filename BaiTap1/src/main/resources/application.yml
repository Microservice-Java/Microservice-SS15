spring:
  application:
    name: two-phase-commit-ticket-booking
  datasource:
    url: jdbc:h2:mem:twopc_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.storex.twopc: DEBUG
